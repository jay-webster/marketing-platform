[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "marketing-platform"
version = "0.1.0"
dependencies = [
    "fastapi",
    "uvicorn",
    "starlette",
]